import React from "react";
import ReactDOM from "react-dom/client";
import EmojiRating from "./EmojiRating/EmojiRating"; // ✅ Adjust if the path is different

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EmojiRating />
  </React.StrictMode>
);
